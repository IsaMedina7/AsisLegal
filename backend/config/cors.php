<?php

'paths' => ['api/*', 'sanctum/csrf-cookie'],

'allowed_methods' => ['*'],

'allowed_origins' => ['*'], // Para desarrollo pon '*' (acepta todo). En producción pones tu dominio.

'allowed_origins_patterns' => [],

'allowed_headers' => ['*'],
